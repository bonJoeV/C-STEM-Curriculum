# _config.yml

title: "C-STEM Has Moved to C-STREAM"
description: "This project has moved to bonjoev.github.io/C-STREAM"
markdown: kramdown

# Specify Kramdown settings for Markdown processing
kramdown:
  input: GFM
  auto_ids: true

# GitHub Pages Theme - using minimal for clean redirect page
remote_theme: pages-themes/minimal@v0.2.0

# Site URL
url: "https://bonJoeV.github.io"
baseurl: "/C-STEM-Curriculum"

# Plugins
plugins:
  - jekyll-remote-theme
  - jekyll-sitemap
